apiVersion: v1
kind: Service
metadata:
  name: ahras-service
  namespace: ahras
  labels:
    app: ahras
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: ahras
